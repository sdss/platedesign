setupRequired(photoop)
setupOptional(platelist)
setupOptional(plate)
setupOptional(fitsverify)
setupOptional(mangacore)
setupOptional(gaia)
setupOptional(sdssdb)
setupenv()
envPrepend(PATH, ${UPS_PROD_DIR}/bin)
envPrepend(IDL_PATH, +${UPS_PROD_DIR}/pro)
envPrepend(PYTHONPATH, ${UPS_PROD_DIR}/python)
